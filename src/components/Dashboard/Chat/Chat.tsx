import { FC, memo } from 'react';
import styles from './chat.module.css';

const Chat: FC<{}> = ({}) => {
  return <div className={styles.wrapper}>To be created...</div>;
};

export default memo(Chat);
